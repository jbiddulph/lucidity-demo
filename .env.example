# Lucidity workspace origin (no trailing slash, no /api/query path)
LUCIDITY_BASE_URL=https://lucidity-lac.vercel.app

# API key from Lucidity (starts with luc_)
LUCIDITY_API_KEY=

# Set to true to use local fixture schema types + documents
LUCIDITY_USE_MOCK=false
